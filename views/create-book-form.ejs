<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Book</title>

    <style>
        :root {
            --bg: #f5f9ff;
            --card: #ffffff;
            --text: #0f172a;
            --muted: #475569;

            --primary: #2563eb;
            --primary-2: #1d4ed8;

            --border: #dbeafe;
            --shadow: 0 10px 25px rgba(15, 23, 42, 0.08);
            --shadowHover: 0 14px 30px rgba(37, 99, 235, 0.18);

            --radius: 14px;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
            background: var(--bg);
            color: var(--text);
            line-height: 1.4;
        }

        .container {
            max-width: 720px;
            margin: 0 auto;
            padding: 24px;
        }

        .topbar {
            margin-bottom: 18px;
        }

        .btn {
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            padding: 10px 12px;
            border-radius: 10px;
            border: 1px solid var(--border);
            background: var(--card);
            color: var(--text);
            cursor: pointer;
        }

        .btn:hover {
            box-shadow: var(--shadowHover);
            border-color: rgba(37, 99, 235, 0.35);
        }

        .card {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            padding: 20px;
        }

        h1 {
            margin: 0 0 6px 0;
            letter-spacing: -0.02em;
        }

        .subtitle {
            color: var(--muted);
            margin-bottom: 18px;
        }

        form {
            display: grid;
            gap: 16px;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 6px;
        }

        input,
        textarea {
            width: 100%;
            padding: 10px 12px;
            border-radius: 10px;
            border: 1px solid var(--border);
            font-size: 1rem;
        }

        input:focus,
        textarea:focus {
            outline: none;
            border-color: var(--primary);
        }

        textarea {
            resize: vertical;
        }

        .hint {
            color: var(--muted);
            font-size: 0.9rem;
            margin-top: 4px;
        }

        .actions {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-top: 10px;
        }

        .btn-primary {
            background: var(--primary);
            border-color: var(--primary);
            color: #fff;
        }

        .btn-primary:hover {
            background: var(--primary-2);
            border-color: var(--primary-2);
        }

        .alert {
          border-radius: 10px;
          padding: 10px 12px;
          font-size: 14px;
          margin-bottom: 12px;
        }

        .alert-error {
          background: #fdecec;
          border: 1px solid #f5c2c7;
          color: #842029;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="topbar">
            <a class="btn" href="/books">← Back to Books</a>
        </div>

        <div class="card">
            <h1>Add Book</h1>
            <div class="subtitle">Fill in the details below to add a new book.</div>
            <% if (typeof error !=="undefined" ) { %>
                <div class="alert alert-error">
                    <%= error %>
            </div>
            <% } %>
            <form method="POST" action="/books/new">
                <div>
                    <label for="BookName">Title</label>
                    <input type="text" id="BookName" name="BookName" required />
                </div>

                <div>
                    <label for="BookAuthor">Author</label>
                    <input type="text" id="BookAuthor" name="BookAuthor" required />
                </div>

                <div>
                    <label for="BookImage">Image URL</label>
                    <input type="text" id="BookImage" name="BookImage" />
                    <div class="hint">
                        Use a direct image URL (must end with .jpg, .png, etc.).
                    </div>
                </div>

                <div>
                    <label for="Description">Description</label>
                    <textarea id="Description" name="Description" rows="5" required></textarea>
                </div>

                <div class="actions">
                    <button class="btn btn-primary" type="submit">Create Book</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>